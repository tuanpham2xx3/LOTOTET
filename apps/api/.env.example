# ===========================================
# LOTOTET Backend Environment Variables
# ===========================================

# Server Config
PORT=3010
NODE_ENV=development

# ===========================================
# Redis Configuration
# ===========================================
# For development: Use standalone mode (no Redis or local Redis)
# For production: Use Sentinel mode

# Standalone Mode (development)
# Leave REDIS_SENTINELS empty to use standalone mode
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Sentinel Mode (production)
# Uncomment and configure for production
# REDIS_SENTINELS=sentinel1.example.com:26379,sentinel2.example.com:26379,sentinel3.example.com:26379
# REDIS_MASTER_NAME=mymaster
# REDIS_PASSWORD=your_secure_password_here

# ===========================================
# Notes
# ===========================================
# - Without Redis: App uses in-memory storage (single server only)
# - With Redis: App supports multi-server deployment with shared state
# - For Cloudflare Tunnel: Use internal IPs for Redis connections
