/**
 * @lototet/shared - Shared types, schemas, and utilities
 *
 * This package contains all shared code between frontend and backend:
 * - Type definitions (TypeScript interfaces and enums)
 * - Validation schemas (Zod)
 * - Socket.IO event contracts
 * - Error codes
 */

// Core types
export * from './types';

// Error handling
export * from './errors';

// Validation schemas
export * from './schemas';

// Socket.IO events
export * from './events';
